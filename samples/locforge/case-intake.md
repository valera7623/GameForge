# Named case intake (replace Ashen Hollow)

## Offer to indie

Free pilot: localize their CSV to RU/ES/DE with glossary + length QA + Unity/Godot export.  
In exchange: permission to name the game + 1 quote on `/ru/locforge` and `/en/locforge`.

## Checklist

1. Receive CSV (`key,source`) + glossary terms (character/place names).
2. Run live localize (not mock) — save export + QA table.
3. Get written permission (email reply is enough):
   - game name / studio
   - one sentence quote
   - link (Steam/itch optional)
4. Update landing `#case` section (RU + EN) — replace Ashen Hollow metrics with real numbers.
5. Archive report under `samples/locforge/cases/{slug}-report.md`.
6. Set lead `status=won` in outreach CSV.

## Landing copy template (RU)

```
Кейс — {Game Name}
{N} ключей · {W} слов · {langs} · {qa} флагов длины
«{quote}» — {dev name}
```

## Contact

- Feedback form: `/ru/feedback?utm_campaign=lf_ru_pilot`
- Email: gameforge.website@yandex.ru subject `LocForge pilot`
